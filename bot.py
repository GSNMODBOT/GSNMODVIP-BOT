import telebot
from config import TOKEN, ADMIN_ID, CHANNEL, CHANNEL_LINK, CHANNEL_ID
import random
import time

bot = telebot.TeleBot(TOKEN)
bot.skip_pending = True

# ========== دالة تحويل رابط المنشور إلى صورة ==========
def رابط_الصورة(رابط):
    return f"https://t.me/kenan_mod_2/{رابط.split('/')[-1]}?embed=1"

# ========== جميع نسخ ببجي لايت ==========
نسخ_ببجي = {
    "v1": {
        "اسم": "🔥 ببجي لايت نسخة 1 • استقرار تام",
        "حجم": "680 MB",
        "وصف": "• ✅ Anti-Ban 100%\n• ⚡ 60 FPS بدون تقطيع",
        "كود": "https://t.me/kenan_mod_2/37",
        "صورة": "https://t.me/kenan_mod_2/36"
    },
    "v2": {
        "اسم": "⚡ ببجي لايت نسخة 2 • سرعة الأساطير",
        "حجم": "690 MB",
        "وصف": "• 🚀 سرعة تصويب خارقة\n• 🎯 ريكويل 0%\n• 🔫 Aimbot تلقائي",
        "كود": "https://t.me/kenan_mod_2/44",
        "صورة": "https://t.me/kenan_mod_2/39"
    },
    "v3": {
        "اسم": "🎯 ببجي لايت نسخة 3 • للجوالات الضعيفة",
        "حجم": "675 MB",
        "وصف": "• 📱 تشغيل على 2 جيجا رام\n• 🔋 بطارية أقل استهلاك\n• بدون OBB",
        "كود": "https://t.me/kenan_mod_2/47",
        "صورة": "https://t.me/kenan_mod_2/45"
    },
    "v4": {
        "اسم": "💀 ببجي لايت نسخة 4 • سكنات حصرية (قريبًا)",
        "حجم": "غير متاح",
        "وصف": "• 👑 سكنات نادرة\n• شدات مجانية\n• قيد الإعداد",
        "كود": "❌ لا يوجد كود بعد",
        "صورة": "https://t.me/kenan_mod_2/36"
    },
    "v5": {
        "اسم": "🕋 ببجي لايت نسخة عربية كاملة",
        "حجم": "غير متاح",
        "وصف": "• 🌍 واجهة عربية\n• قيد الإعداد",
        "كود": "❌ لا يوجد كود بعد",
        "صورة": "https://t.me/kenan_mod_2/36"
    }
}

# ========== التحقق من الاشتراك ==========
def مشترك(user_id):
    try:
        member = bot.get_chat_member(CHANNEL_ID, user_id)
        return member.status in ['member', 'administrator', 'creator']
    except:
        return False

# ========== ردود ذكية متطورة (للمجموعات والخاص) ==========
@bot.message_handler(func=lambda message: True)
def ردود_ذكية(message):
    if message.text is None:
        return
    text = message.text.strip().lower()
    reply = None

    # ===== تحيات وترحيب =====
    if any(word in text for word in ["السلام", "سلام", "hi", "hello"]):
        reply = "🌹 وعليكم السلام ورحمة الله وبركاته، أهلاً بك في بوت غسان مود 🤍"

    elif any(word in text for word in ["شكرا", "شكرًا", "thx", "thanks"]):
        reply = "🤍 العفو، هذا واجبنا، نورتنا"

    elif any(word in text for word in ["هلا", "هلا والله", "مرحبا"]):
        reply = "🌹 هلا فيك، نورت البوت"

    elif "كيفك" in text or "كيف حالك" in text:
        reply = "الحمدلله تمام، انت كيفك؟"

    elif "اسمك" in text or "من انت" in text:
        reply = "أنا بوت غسان مود 🤖، تحت أمرك"

    elif "مطورك" in text or "مين مطورك" in text:
        reply = "مطوري: @GSN_MOD_1 ❤️"

    # ===== التحميل والنسخ =====
    elif any(word in text for word in ["وين التحميل", "كيف احمل", "رابط التحميل", "ابي العبه"]):
        reply = "⬇️ كل الروابط هنا:\nhttps://t.me/kenan_mod_2\nأو اختر نسخة: /v1, /v2, /v3"

    elif "ملف لعبة" in text or "الملف" in text:
        reply = "📦 جميع الملفات في القناة:\nhttps://t.me/kenan_mod_2"

    # ===== اسكنات وشدات =====
    elif any(word in text for word in ["اسكنات", "سكنات", "skins"]):
        reply = "🎭 نسخة السكنات قيد الإعداد (v4 قريبًا)، لكن v2 فيها بعض السكنات"

    elif any(word in text for word in ["شدات", "shadat"]):
        reply = "💎 شدات مجانية؟ v4 قيد الإعداد. حالياً شدات شراء تحتاج حساب أساسي"

    # ===== دراكون =====
    elif "دراكون" in text or "dragon" in text:
        reply = "🐉 دراكون؟ نسخة السكنات الحصرية قريبًا في v4"

    # ===== هكر / هاكينج =====
    elif any(word in text for word in ["هكر", "hack", "هاكينج", "vip hack"]):
        reply = "🔐 حساب أساسي وهكر؟ هذا صعب ويخالف سياسات ببجي، ننصحك تستخدم نسخة معدلة آمنة فقط"

    # ===== إيمبوت =====
    elif any(word in text for word in ["ايمبوت", "aimbot", "ضرب تلقائي"]):
        reply = "🔫 الإيمبوت (الضرب التلقائي) موجود في v2، جربها:\nhttps://t.me/kenan_mod_2/44"

    # ===== روم ضدي =====
    elif "روم ضدي" in text or "room 1v1" in text:
        reply = "😎 لو تجي روم ضدي، راح تندم"

    # ===== مشاكل التوافق =====
    elif "غير متوافق" in text or "متوافقة مع جهازك" in text or "android 15" in text:
        reply = "📱 إذا جوالك أندرويد 15، ممكن بعض النسخ القديمة ما تشتغل. جرب v3 المخصصة للجوالات الحديثة"

    elif "تطبيق ليس مثبتًا" in text or "لم يتم تثبيت" in text:
        reply = "⚠️ إذا ظهرت رسالة التطبيق غير مثبت، جرب:\n1. شيل النسخة القديمة\n2. ثبت الجديدة\n3. فعّل مصادر غير معروفة"

    elif "خطأ في التثبيت" in text or "فشل التثبيت" in text:
        reply = "🔧 لفشل التثبيت:\n• احذف النسخة القديمة\n• أعد تشغيل الجوال\n• حرر مساحة (2 جيجا على الأقل)\n• حمّل النسخة مرة ثانية"

    elif "نقل ملفات" in text or "مشكلة نقل" in text:
        reply = "📂 مشكلة نقل الملفات؟ استخدم ZArchiver وانقل الملف من التليجرام إلى مجلد Downloads"

    # ===== مساحة التخزين =====
    elif "مساحة" in text or "مساحه" in text or "ذاكرة" in text:
        reply = "🗂️ تأكد أن مساحة التخزين كافية (2 جيجا على الأقل)، واحذف الملفات القديمة"

    # ===== 32 بت =====
    elif "32 بت" in text or "32bit" in text:
        reply = "✅ جوالات 32 بت تشتغل مع v3، جربها"

    # ===== 64 بت =====
    elif "64 بت" in text or "64bit" in text:
        reply = "⚡ جوالات 64 بت تشتغل مع v2 و v3 بكل سلاسة"

    # ===== ملف obb =====
    elif "obb" in text:
        reply = "📁 v3 بدون OBB، v2 و v1 تحتاج OBB. قريبًا رفعه في القناة"

    # ===== كود نسخة =====
    elif "كود النسخه" in text or "كود رقم" in text or "ابي كود" in text:
        reply = "🔢 إذا تبي كود نسخة معينة، اكتب /code متبوعًا برقم النسخة، مثال:\n/code 2"

    # ===== حل مشاكل عام =====
    elif "حل مشكله" in text or "مشكله" in text:
        reply = "🛠️ اذكر المشكلة بالتفصيل، وحنحاول نساعدك"

    if reply:
        bot.reply_to(message, reply)

# ========== أمر code: يجيب كود نسخة ==========
@bot.message_handler(commands=['code'])
def كود_نسخة(message):
    try:
        رقم = int(message.text.split()[1])
        if رقم == 1:
            bot.reply_to(message, "📎 كود نسخة 1:\nhttps://t.me/kenan_mod_2/37")
        elif رقم == 2:
            bot.reply_to(message, "📎 كود نسخة 2:\nhttps://t.me/kenan_mod_2/44")
        elif رقم == 3:
            bot.reply_to(message, "📎 كود نسخة 3:\nhttps://t.me/kenan_mod_2/47")
        else:
            bot.reply_to(message, "❌ هذا الرقم غير موجود")
    except:
        bot.reply_to(message, "⚠️ استخدم: /code رقم النسخة (1,2,3)")

# ========== أمر start الرئيسي ==========
@bot.message_handler(commands=['start'])
def start(message):
    uid = message.from_user.id
    name = message.from_user.first_name

    if len(message.text.split()) > 1:
        كود = message.text.split()[1]
        if كود in نسخ_ببجي:
            v = نسخ_ببجي[كود]
            if مشترك(uid):
                if v['كود'] == "❌ لا يوجد كود بعد":
                    bot.reply_to(message, f"⏳ {v['اسم']}\n📦 {v['حجم']}\n{v['وصف']}\n⚠️ لا يوجد كود بعد، جرب لاحقًا")
                else:
                    bot.send_message(uid, f"{v['اسم']}\n━━━━━━━━━━━━━━\n📦 {v['حجم']}\n{v['وصف']}\n━━━━━━━━━━━━━━\n⬇️ {v['كود']}", disable_web_page_preview=False)
            else:
                زر = telebot.types.InlineKeyboardMarkup()
                زر.add(telebot.types.InlineKeyboardButton("📢 اشترك", url=CHANNEL_LINK))
                bot.send_photo(uid, photo=رابط_الصورة(v['صورة']), caption=f"{v['اسم']}\n━━━━━━━━━━━━━━\n📦 {v['حجم']}\n{v['وصف']}\n━━━━━━━━━━━━━━\n⚠️ اشترك أولاً", reply_markup=زر)
    else:
        if مشترك(uid):
            زر = telebot.types.InlineKeyboardMarkup(row_width=1)
            for k, v in نسخ_ببجي.items():
                زر.add(telebot.types.InlineKeyboardButton(f"{v['اسم']}", url=f"https://t.me/GSNMODBOT?start={k}"))
            bot.reply_to(message, f"🌹 مرحبًا {name}\nاختر نسختك:", reply_markup=زر)
        else:
            زر = telebot.types.InlineKeyboardMarkup()
            زر.add(telebot.types.InlineKeyboardButton("📢 اشترك", url=CHANNEL_LINK))
            bot.send_photo(uid, photo=رابط_الصورة("https://t.me/kenan_mod_2/36"), caption=f"⚠️ اشترك أولًا يا {name}", reply_markup=زر)

# ========== أمر عرض جميع النسخ ==========
@bot.message_handler(commands=['نسخ', 'versions'])
def عرض_النسخ(message):
    text = "📋 **جميع النسخ المتوفرة:**\n\n"
    for key, val in نسخ_ببجي.items():
        if val['كود'] != "❌ لا يوجد كود بعد":
            text += f"✅ {val['اسم']} – {val['حجم']}\n{val['وصف']}\n📎 {val['كود']}\n\n"
        else:
            text += f"⏳ {val['اسم']} – {val['حجم']}\n{val['وصف']}\n⚠️ {val['كود']}\n\n"
    bot.reply_to(message, text, parse_mode="Markdown")

# ========== أوامر حجم النسخة الفردية ==========
@bot.message_handler(commands=['v1', 'v2', 'v3', 'v4', 'v5'])
def حجم_نسخة(message):
    cmd = message.text[1:]
    if cmd in نسخ_ببجي:
        v = نسخ_ببجي[cmd]
        if v['كود'] == "❌ لا يوجد كود بعد":
            bot.reply_to(message, f"⏳ {v['اسم']}\n📦 {v['حجم']}\n{v['وصف']}\n⚠️ لا يوجد كود بعد")
        else:
            bot.reply_to(message, f"{v['اسم']}\n📦 {v['حجم']}\n{v['وصف']}\n📎 {v['كود']}")
    else:
        bot.reply_to(message, "❌ هذه النسخة غير موجودة")

# ========== أمر نسخة عشوائية ==========
@bot.message_handler(commands=['random'])
def نسخة_عشوائية(message):
    المتاحة = [v for v in نسخ_ببجي.values() if v['كود'] != "❌ لا يوجد كود بعد"]
    if المتاحة:
        v = random.choice(المتاحة)
        bot.reply_to(message, f"🎲 اخترت لك:\n{v['اسم']}\n📎 {v['كود']}")
    else:
        bot.reply_to(message, "⏳ لا توجد نسخ متاحة حاليًا")

# ========== تشغيل البوت ==========
if __name__ == "__main__":
    print("🚀 البوت الذكي الخارق شغال ✅")
    while True:
        try:
            bot.polling(none_stop=True)
        except Exception as e:
            print(f"خطأ: {e}")
            time.sleep(3)
